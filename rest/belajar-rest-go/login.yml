info:
  name: login
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:8080/login/
  body:
    type: json
    data: |-
      {
        "email" : "ikbal@tes.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
