info:
  name: signup user
  type: http
  seq: 5

http:
  method: POST
  url: 0.0.0.0:8080/signup/
  body:
    type: json
    data: |-
      {
        "email" : "ikbal@tes.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
