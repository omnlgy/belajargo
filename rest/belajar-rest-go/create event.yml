info:
  name: create event
  type: http
  seq: 3

http:
  method: POST
  url: 0.0.0.0:8080/events/
  body:
    type: json
    data: |-
      {
        "description": "tes description2",
        "dateTime": "2026-01-02T15:04:05Z",
        "description": "tes desctiption midleware body validation",
        "name": "body validation",
        "location": "middleware"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
